import { Env.Service.Provider } from './env.service.provider';

describe('Env.Service.Provider', () => {
  it('should create an instance', () => {
    expect(new Env.Service.Provider()).toBeTruthy();
  });
});
